# Vercel Deployment
# Backend API URL (without trailing slash)
VITE_API_URL=https://your-backend.vercel.app

# Optional: Google OAuth Client ID (if using real Google login)
# VITE_GOOGLE_CLIENT_ID=your_google_client_id
